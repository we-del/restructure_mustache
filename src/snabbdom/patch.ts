export function patch(oldVNode: any, newVNode: any) {
    if (!oldVNode && !newVNode) {
        console.log('参数不能为空 ')
        return
    }
    // 第一次上树
    if (oldVNode instanceof Element) {

    }

}